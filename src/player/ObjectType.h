#ifndef OBJECTTYPE_H_
#define OBJECTTYPE_H_

// enum ObjectType is used to identyfy a GameObject's type (i.e. ship class, type of projectile, obstacle, ...)
enum ObjectType
{
	SHIP1,
	SHIP2,
        SHIP3,
	PROJECTILE,
	OBSTACLE,
	POWERUP
};

#endif
